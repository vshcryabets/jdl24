#include "Controller.h"

// Controller is a pure abstract interface; all members are declared inline in
// the header. This translation unit exists to give the interface a compilation
// home and to keep it referenced by the dl24cpp target.
